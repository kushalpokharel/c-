#include <iostream>
using namespace std;

double fact(int a)
{
    if(a<0)
        return 0;
    else if(a<2)
        return 1;
    else
        return a*fact(a-1);
}
int main()
{
    int x;
    cin>>x;
    cout<<fact(x);

}
